export class VehicleTypes {

    group: string;
    type: string;
    model: string;

    constructor(group: string, type: string, model: string) {

        this.group = group;
        this.type = type;
        this.model = model;
    }
}